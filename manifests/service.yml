apiVersion: v1
kind: Service
metadata:
    name: ssmbclouddockercomposemultiplatform
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: ssmbclouddockercomposemultiplatform